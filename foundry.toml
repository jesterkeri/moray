[profile.default]
src = "src"
out = "out"
test = "test"
libs = ["lib"]
solc = "0.8.24"
optimizer = true
optimizer_runs = 200

# Fill these in when you deploy to Monad testnet:
# [rpc_endpoints]
# monad_testnet = "${MONAD_TESTNET_RPC}"
#
# Deploy key comes from an env var, never commit it:
#   forge script ... --rpc-url monad_testnet --private-key $DEPLOYER_KEY --broadcast
